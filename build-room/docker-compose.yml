version: '3'

services:
  amazonfreertos-esp32:
    image: axetay-buildroom-amazonfreertos-esp32:latest
    build:
      context: .
      dockerfile: Dockerfile
    volumes: 
        - ../build-output/:/build-output
        - ../project-code/:/project-code
